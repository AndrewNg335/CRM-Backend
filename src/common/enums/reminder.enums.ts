export enum Repeat {
    NEVER = 'NEVER',
    DAILY = 'DAILY',
    WEEKLY = 'WEEKLY',
    MONTHLY = 'MONTHLY',
  }
  
  export enum Priority {
    LOW = 'LOW',
    MEDIUM = 'MEDIUM',
    HIGH = 'HIGH',
  }
  
  export enum ReminderStatus {
    PENDING = 'PENDING',
    DONE = 'DONE',
    CANCELLED = 'CANCELLED',
  }
  
  